-- 🌸 MIYU HUB V5 VIP 🌸

local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")

------------------------------------------------
-- SCREEN GUI
------------------------------------------------

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MiyuHub"
ScreenGui.Parent = game.CoreGui

------------------------------------------------
-- BLUR EFFECT (VIP STYLE)
------------------------------------------------

local blur = Instance.new("BlurEffect")
blur.Size = 18
blur.Parent = Lighting

------------------------------------------------
-- MAIN MENU
------------------------------------------------

local Main = Instance.new("Frame")
Main.Parent = ScreenGui
Main.Size = UDim2.new(0,520,0,360)
Main.Position = UDim2.new(0.5,-260,0.5,-180)
Main.BackgroundColor3 = Color3.fromRGB(35,35,45)
Main.BackgroundTransparency = 0.25

local Corner = Instance.new("UICorner")
Corner.Parent = Main
Corner.CornerRadius = UDim.new(0,12)

local Stroke = Instance.new("UIStroke")
Stroke.Parent = Main
Stroke.Color = Color3.fromRGB(255,120,200)
Stroke.Thickness = 2

------------------------------------------------
-- TOP BAR
------------------------------------------------

local TopBar = Instance.new("Frame")
TopBar.Parent = Main
TopBar.Size = UDim2.new(1,0,0,40)
TopBar.BackgroundColor3 = Color3.fromRGB(255,120,200)

local Title = Instance.new("TextLabel")
Title.Parent = TopBar
Title.Size = UDim2.new(1,0,1,0)
Title.Text = "🌸 Miyu Hub 🌸"
Title.BackgroundTransparency = 1
Title.TextScaled = true
Title.Font = Enum.Font.GothamBold
Title.TextColor3 = Color3.new(1,1,1)

------------------------------------------------
-- ANIME LOGO
------------------------------------------------

local Logo = Instance.new("ImageLabel")
Logo.Parent = Main
Logo.Size = UDim2.new(0,80,0,80)
Logo.Position = UDim2.new(0,10,0,50)
Logo.BackgroundTransparency = 1
Logo.Image = "rbxassetid://96179046857812"

------------------------------------------------
-- MOVING BACKGROUND EFFECT
------------------------------------------------

local Bg = Instance.new("Frame")
Bg.Parent = Main
Bg.Size = UDim2.new(1,0,1,0)
Bg.BackgroundTransparency = 1

local Particle = Instance.new("ParticleEmitter")
Particle.Parent = Bg
Particle.Texture = "rbxassetid://243660364"
Particle.Rate = 6
Particle.Lifetime = NumberRange.new(2)
Particle.Speed = NumberRange.new(1)
Particle.Color = ColorSequence.new(Color3.fromRGB(255,120,200))
Particle.Size = NumberSequence.new(0.25)

------------------------------------------------
-- AUTO FARM BUTTON
------------------------------------------------

local AutoFarm = false

local FarmButton = Instance.new("TextButton")
FarmButton.Parent = Main
FarmButton.Size = UDim2.new(0,200,0,50)
FarmButton.Position = UDim2.new(0.5,-100,0.55,0)
FarmButton.Text = "Auto Farm"
FarmButton.BackgroundColor3 = Color3.fromRGB(255,182,193)
FarmButton.TextColor3 = Color3.new(1,1,1)
FarmButton.TextScaled = true
FarmButton.Font = Enum.Font.GothamBold

FarmButton.MouseButton1Click:Connect(function()

AutoFarm = not AutoFarm

FarmButton.Text = AutoFarm and "Auto Farm: ON" or "Auto Farm: OFF"

while AutoFarm do
task.wait()

-- bạn có thể thêm code farm mob ở đây

end

end)

------------------------------------------------
-- AUTO BOSS BUTTON
------------------------------------------------

local AutoBoss = false

local BossButton = Instance.new("TextButton")
BossButton.Parent = Main
BossButton.Size = UDim2.new(0,200,0,50)
BossButton.Position = UDim2.new(0.5,-100,0.75,0)
BossButton.Text = "Auto Boss"
BossButton.BackgroundColor3 = Color3.fromRGB(255,182,193)
BossButton.TextColor3 = Color3.new(1,1,1)
BossButton.TextScaled = true
BossButton.Font = Enum.Font.GothamBold

BossButton.MouseButton1Click:Connect(function()

AutoBoss = not AutoBoss

BossButton.Text = AutoBoss and "Auto Boss: ON" or "Auto Boss: OFF"

while AutoBoss do
task.wait()

-- thêm code đánh boss ở đây

end

end)
